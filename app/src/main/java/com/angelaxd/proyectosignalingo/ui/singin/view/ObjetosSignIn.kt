package com.angelaxd.proyectosignalingo.ui.singin.view

import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController

/**
 * Nota: esto ya no se uso
 * **/

@Composable
fun SignInBoton(navController: NavHostController,
                //state: SingInState,
                //onSignInClick: () -> Unit


                ){

    /*val context = LocalContext.current
    LaunchedEffect(key1 = state.singInError ){
        state.singInError?.let { error->
            Toast.makeText(
                context,
                error,
                Toast.LENGTH_LONG
            ).show()

        }
    }*/


}